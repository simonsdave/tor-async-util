language: python
python:
  - "2.7"
install:
  - source cfg4dev
  - pip install coveralls
script:
  - pycodestyle
  - flake8
  - python setup.py sdist --formats=gztar
  - nosetests --with-coverage --cover-branches --cover-erase --cover-package tor_async_util
after_success:
  - coveralls || true
